<!DOCTYPE html>
<html>
<head>
	<title>Tasks</title>
	<style>
		body {
			/* next three lines courtesy of https://jrl.ninja/etc/1/ */
			max-width: 70ch;
			padding: 2ch;
			margin: auto;

			line-height: 1.4;
			font:100%/1.5 -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Tahoma, 'Helvetica Neue', Helvetica, Arial, sans-serif;
		}
		table {
			border: solid 1px #DDEEEE;
			border-collapse: collapse;
			border-spacing: 0;
		}
		th {
			background-color: #DDEFEF;
			text-align:left;
		}
		td {
			background-color:#eee;
			border: solid 1px #bcc;
		}
		table * { 
			padding:8px;
		}
	</style>
</head>
<body>
	<table>
		<thead>
			<tr>
				<th>ID</th>
				<th>Priority</th>
				<th>Summary</th>
				<th>Due</th>
			</tr>
		</thead>
		<tbody>
			{% for task in tasks %}
			<tr>
				<td>{{task.id}}</td>
				<td>{{task.priority}}</td>
				<td>{{task.summary}}</td>
				<td>{{task.due}}</td>
			</tr>
			{% endfor %}
		</tbody>
	</table>
</body>

